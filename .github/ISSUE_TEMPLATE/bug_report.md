---
name: Bug report
about: Something isn't working as expected
title: ''
labels: bug
assignees: ''
---

**What happened?**
A clear description of the bug.

**Steps to reproduce**
1.
2.
3.

**Expected behavior**
What should have happened instead.

**Environment**
- OS: (e.g., Windows 11, macOS 14, Ubuntu 24.04)
- Python version: (run `python --version`)
- resonant-archive version: (run `resonant-archive --version`)
- Install method: (`pip install`, `git clone + pip install -e .`, `uv`, other)
- Embedding model: (default `all-MiniLM-L6-v2`, or custom)

**Command that failed**
The exact command you ran, including flags and arguments.

**Logs / error output**
Paste the full terminal output, including any traceback. If it's very long, consider a [gist](https://gist.github.com).

**Archive size (if relevant)**
- Number of files in your source directory:
- `resonant-archive stats` output (if the archive exists):
